{
    "version": "0.2.0",
    "env": {
        "CARGO_MANIFEST_DIR": "${workspaceFolder}/assets",
    },
    "configurations": [
        {
            "name": "Debug",
            "type": "lldb",
            "request": "launch",
            "cargo": {
                "args": [
                    "build",
                    "--package=bevy"
                ],
                "filter": {
                    "name": "base_game",
                }
            },
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}